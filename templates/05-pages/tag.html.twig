{% extends "04-templates/base.html.twig" %}

{% block title %}Tag {{ tag.name }}{% endblock %}
{% block content %}
    <h1>Tag {{ tag.name }}</h1>
    <div class="card-columns">
        {% for component in components %}
            {% include '03-organisms/component-card.html.twig' with { 'component': component } %}
        {% endfor %}
    </div>
{% endblock %}